import { SchedulingEndedEvent } from './scheduling-ended-event';

describe('SchedulingEndedEvent', () => {
  it('should create an instance', () => {
    expect(new SchedulingEndedEvent()).toBeTruthy();
  });
});
