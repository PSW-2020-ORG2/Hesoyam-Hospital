import { TimeIntervalFilter } from './time-interval-filter';

describe('TimeIntervalFilter', () => {
  it('should create an instance', () => {
    expect(new TimeIntervalFilter()).toBeTruthy();
  });
});
