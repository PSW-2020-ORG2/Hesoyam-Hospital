import { SearchCriteriaDTO } from './search-criteria-dto';

describe('SearchCriteriaDTO', () => {
  it('should create an instance', () => {
    expect(new SearchCriteriaDTO()).toBeTruthy();
  });
});
