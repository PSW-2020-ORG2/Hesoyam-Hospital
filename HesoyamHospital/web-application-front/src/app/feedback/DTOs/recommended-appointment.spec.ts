import { RecommendedAppointment } from './recommended-appointment';

describe('RecommendedAppointment', () => {
  it('should create an instance', () => {
    expect(new RecommendedAppointment()).toBeTruthy();
  });
});
